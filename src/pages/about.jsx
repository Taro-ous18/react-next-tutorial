import { Header } from "@/components/Header";
import Head from "next/head";

const about = () => {
  return (
    <div>
      <Head>
        <title>about</title>
      </Head>
      <Header />
    </div>
  );
};

export default about;
